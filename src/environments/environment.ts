/**
 * @typedef {{production: boolean}} Environment
 * @desc 環境設定 ( types/environment.d.ts )
 */

/**
 * 環境設定
 * @type {Environment}
 */
export const environment: Environment = {
  production: false
};
